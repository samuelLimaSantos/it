import styled from 'styled-components/native';

export const Container = styled.View`
  position: absolute;
  background-color: rgba(0,0,0,0.9);
  width: 100%;
  height: 100%;
  z-index: 9999;
  justify-content: center;
`;

export const LoadingComponent = styled.ActivityIndicator``;